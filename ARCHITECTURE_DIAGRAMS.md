# Architecture Diagrams

Visual representations of the Random Number Generator application architecture and flows.

---

## 📊 System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         WEB BROWSER                              │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                    PRESENTATION LAYER                       │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌─────────────────┐  │ │
│  │  │ index.html   │  │  style.css   │  │  Result Display │  │ │
│  │  │              │  │              │  │                 │  │ │
│  │  │ - Input      │  │ - Layout     │  │ - Number Grid   │  │ │
│  │  │ - Buttons    │  │ - Styling    │  │ - Progress Bar  │  │ │
│  │  └──────────────┘  └──────────────┘  └─────────────────┘  │ │
│  └──────────────────────────┬─────────────────────────────────┘ │
│                             │                                    │
│  ┌──────────────────────────▼─────────────────────────────────┐ │
│  │                    BUSINESS LOGIC LAYER                     │ │
│  │                      (script.js)                            │ │
│  │  ┌─────────────────┐  ┌──────────────┐  ┌──────────────┐  │ │
│  │  │ Event Handlers  │  │  Validation  │  │   Display    │  │ │
│  │  └─────────────────┘  └──────────────┘  └──────────────┘  │ │
│  └─────────────────────────────┬────────────────────────────┘  │
│                                │                                │
│  ┌────────────────────────────▼─────────────────────────────┐  │
│  │                  PROCESSING LAYER                         │  │
│  │                   (worker.js)                             │  │
│  │  ┌──────────────────────────────────────────────────────┐│  │
│  │  │         Web Worker Thread                            ││  │
│  │  │  - Combination Generation                            ││  │
│  │  │  - Batch Processing                                  ││  │
│  │  └──────────────────────────────────────────────────────┘│  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Single Generation Flow

```
User clicks "Gerar Números"
  ↓
Parse & Validate fixed numbers
  ↓
Calculate numbers needed (15 - fixed count)
  ↓
generateUniqueRandomNumbers()
  ↓
Fisher-Yates Shuffle
  ↓
Combine with fixed numbers
  ↓
Sort final set
  ↓
Add to historyOfGeneratedSets
  ↓
displayAllSets()
  ↓
END
```

---

## 🔌 Web Worker Communication

```
MAIN THREAD                           WORKER THREAD
    │                                      │
    │ postMessage({                        │
    │   availableNumbers,                  │
    │   numbersToChoose,                   │
    │   fixedNumbers                       │
    │ })                                   │
    ├─────────────────────────────────────>│
    │                                      │
    │                                      │ Generate
    │                                      │ Combinations
    │                                      │
    │                 postMessage({        │
    │                   type: 'batch',     │
    │                   combinations: [...],
    │                   count: 1000        │
    │                 })                   │
    │<─────────────────────────────────────┤
    │                                      │
    │ Update progress                      │
    │                                      │
    │                 postMessage({        │
    │                   type: 'complete'   │
    │                 })                   │
    │<─────────────────────────────────────┤
    │                                      │
    │ Terminate worker                     │
    │                                      │
    ▼                                      ▼
  END                                    END
```

---

## 🎯 State Machine

```
IDLE
  ↓ (Click "Gerar Números")
GENERATING_SINGLE
  ↓ (Instant)
DISPLAYING
  ↓
IDLE

IDLE
  ↓ (Click "Gerar Tudo" + Confirm)
GENERATING_ALL
  ↓ (Complete or Cancel)
DISPLAYING
  ↓
IDLE
```

---

## 📊 Data Flow

```
USER INPUT "1, 5, 10"
  ↓
Split & Parse → [1, 5, 10]
  ↓
Validate (range, duplicates, count)
  ↓
Calculate Available → [2,3,4,6,7,8,9,11...]
  ↓
Generate Random/Combinations
  ↓
Combine [...fixed, ...generated]
  ↓
Sort
  ↓
Store in historyOfGeneratedSets[]
  ↓
Render to DOM
  ↓
DISPLAY
```

---

## 🌳 Fisher-Yates Shuffle

```
Initial: [2, 3, 4, 6, 7, 8, 9]

Shuffle:
  i=6: swap[6] with random[0-6]
  i=5: swap[5] with random[0-5]
  i=4: swap[4] with random[0-4]
  ...
  i=1: swap[1] with random[0-1]

Result: [8, 6, 9, 3, 7, 2, 4]
Take first N elements
```

---

## 🧩 Component Hierarchy

```
<body>
  └─ <div class="container">
       ├─ <h1>
       ├─ <div class="input-group">
       │   └─ <input id="fixedNumbers">
       ├─ <button id="generateButton">
       ├─ <button id="generateAllButton">
       └─ <div id="result">
            └─ <div class="number-line"> × N
                 └─ <span class="number-item"> × 15
```

---

**Version:** 1.0  
**Last Updated:** 2025-10-17
